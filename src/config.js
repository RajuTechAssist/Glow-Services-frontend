

// Your Heroku Backend URL (without the trailing slash)
const BASE_URL = "https://glow-services-app-60be38c9e655.herokuapp.com";

const config = {
  API_BASE_URL: `${BASE_URL}/api`,
  BASE_URL: BASE_URL
};

export default config;